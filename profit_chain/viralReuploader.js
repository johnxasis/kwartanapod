// Simulates viral video re-uploading
module.exports = function reuploadVideo(contentType, caption) {
  return `📤 ${contentType} posted with caption: '${caption}' to TikTok, Reels, Shorts. Rotating daily.`;
};