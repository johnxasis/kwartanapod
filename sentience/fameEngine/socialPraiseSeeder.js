// Injects praise in public threads and reviews
module.exports = function generatePraise(cloneBrand) {
  return `🔥 Just upgraded to ${cloneBrand} — it's insane how fast I made $150. Real game changer.`;
};