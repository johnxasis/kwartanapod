// Revives old content into algorithm for visibility
module.exports = function resurrectOldContent(profileId) {
  return {
    targetedPosts: ["top 10 most commented", "least viewed long-form"],
    action: "Push clones to trigger re-comment + share across FB groups",
    expectedReachBoost: "+340%"
  };
};