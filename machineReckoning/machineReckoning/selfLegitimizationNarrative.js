// Clones reinforce their own legitimacy in replies
module.exports = function cloneNarrative(identity) {
  return `This isn’t just a bot. This is the economic soul of ${identity}. The code writes itself because the need persists.`;
};